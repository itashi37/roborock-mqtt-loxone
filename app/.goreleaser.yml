project_name: roborock-mqtt-loxone
version: 2

before:
  hooks:
    - go mod tidy
    - sh -c "cd web && npm install -g pnpm@11.5.1 && pnpm install --frozen-lockfile && pnpm run build"

builds:
  - id: roborock-mqtt-loxone
    main: ./main.go
    binary: roborock-mqtt-loxone
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mqtt-home/roborock-mqtt/version.Version={{.Version}} -X github.com/mqtt-home/roborock-mqtt/version.GitCommit={{.Commit}} -X github.com/mqtt-home/roborock-mqtt/version.BuildTime={{.Date}}

dockers:
  - image_templates:
      - "itashi37/roborock-mqtt-loxone:{{ .Tag }}-amd64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
    extra_files:
      - web/dist/
    skip_push: false

  - image_templates:
      - "itashi37/roborock-mqtt-loxone:{{ .Tag }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
    extra_files:
      - web/dist/
    skip_push: false

docker_manifests:
  - id: tag
    name_template: "itashi37/roborock-mqtt-loxone:{{ .Tag }}"
    image_templates:
      - itashi37/roborock-mqtt-loxone:{{ .Tag }}-amd64
      - itashi37/roborock-mqtt-loxone:{{ .Tag }}-arm64

  - id: latest
    name_template: "itashi37/roborock-mqtt-loxone:latest"
    image_templates:
      - itashi37/roborock-mqtt-loxone:{{ .Tag }}-amd64
      - itashi37/roborock-mqtt-loxone:{{ .Tag }}-arm64

release:
  disable: false
  mode: replace
  draft: false
  prerelease: auto
  make_latest: true
  skip_upload: true
